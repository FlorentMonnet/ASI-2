export const selectorUserConnected = (state) => state.userConnected;

export const selectorAdverseUser = (state) => state.opponentInGame;

export const selectUser = (state) => state.userSelected;

export const selectorUsers = (state) => state.users;

export const selectorTurnUserId = (state) => state.turnGameUserId;

export const selectorUserPointInGame = (state) => state.userPointInGame;
